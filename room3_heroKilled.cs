using UnityEngine;

public class room3_heroKilled : MonoBehaviour
{
    public GameObject Hero;


   /* void Start()
    {
        //UnityEngine.Object.FindObjectsOfType(ScoreValueParsing);
        Hero = GameObject.Find("Human Male (v4)");
        if (Hero)
        {
            Hero.GetComponent<room3_HealthScript>();
            bool Health = room3_HealthScript.
            Debug.Log("Score X from ObjectFinder3 is " + ScoreX);

            star = (int)ScoreX;
        }

    }*/

    // Update is called once per frame
    void Update()
    {
        
    }




}
